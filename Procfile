# Backend service (default)
web: uvicorn app.backend.main_simple:app --host 0.0.0.0 --port $PORT

# Frontend service (add as separate service in Railway)
# Use start command: streamlit run app/frontend.py --server.port $PORT --server.address 0.0.0.0 --server.headless true

# Data generator service (add as separate service in Railway)
# Use start command: python app/data_generator/generator_simple.py
